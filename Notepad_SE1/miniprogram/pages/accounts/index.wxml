<view>
  <view class="topBar" style="display: flex;text-align: left;">

    <view style="font-size: 45rpx;margin-top: 20rpx;">日期
      <picker
        style="font-size: 35rpx; width: 158rpx; height: 106rpx; display: block; box-sizing: border-box; left: NaNrpx; top: NaNrpx">
        日期选择{{pickDate}}</picker>
    </view>
    <view
      style="margin-left: 10%; font-size: 30rpx; margin-top: 55rpx; width: 158rpx; height: 141rpx; display: block; box-sizing: border-box; left: NaNrpx; top: NaNrpx">
      <text>本日支出\n{{dayCost}}¥</text>
    </view>
    <view style="margin-left: 10%;font-size:30rpx;margin-top:55rpx"><text>本日收入\n{{dayIncome}}¥</text></view>

  </view>
  <view class="topBar">
    <mp-sticky offset-top="64">
      <button size="mini" type="primary" style="margin-left: 115px; background-color: #008F7A"
        bindtap="writeABillPopup">记一笔</button>
    </mp-sticky>
  </view>
</view>

<view style="height: 20px;background-color: gainsboro;">分隔栏</view>

<view>记账页面</view>
<mp-cells title="这里写日期">
  <!-- <mp-slideview buttons="{{[1]}}">
    <mp-cell footer="说明文字">
      <mp-icon icon="add2" color="black" size="{{25}}"></mp-icon>
      首条记录
    </mp-cell>
  </mp-slideview> -->
  <mp-cell value="第一条记录">
    <mp-icon icon="link" color="black" size="{{25}}"></mp-icon>

  </mp-cell>
</mp-cells>
<mp-cells title="这里写日期too">
  <mp-cell>第二条记录</mp-cell>
</mp-cells>

<page-container show="{{show}}" round="{{round}}" overlay="{{overlay}}" duration="{{duration}}" position="{{position}}"
  close-on-slide-down="{{false}}" bindbeforeenter="onBeforeEnter" bindenter="onEnter" bindafterenter="onAfterEnter"
  bindbeforeleave="onBeforeLeave" bindleave="onLeave" bindafterleave="onAfterLeave" bindclickoverlay="onClickOverlay"
  custom-style="{{customStyle}}" overlay-style="{{overlayStyle}}">
  <view style="background-color: #ABD9FF;">
    <view class="titleHeader">日期</view>
    <view>
      <picker mode="date" value="{{date}}" start="2000-07-14" end="{{today}}" bindchange="bindDateChange">
        <view style="display: flex;">
          <view style="flex-grow: 4;margin-left: 20rpx;">
            {{date}}</view>
            <view style="flex-grow: 0.5;background-color: #85d6ff;margin-right: 20rpx;border:3px solid #4090f8;border-radius:10px;text-align: center;font-size: smaller;color: white;font:bold" wx:if="{{date==today}}">今天</view>
          
        </view>
      </picker>
    </view>
    <view class="titleHeader">请选择账目类型</view>
    <view class="HContainer" >
      <button bindtap="selCost" style='background-color: {{costOrIncome=="cost"? "#009CE9" : "white" }}'>支出</button>
      <button bindtap="selIncome" style='background-color: {{costOrIncome=="income"? "#009CE9" : "white" }}'>收入</button>
    </view>
    <view class="titleHeader">类别</view>
    <view class="typeBox" wx:if='{{costOrIncome=="cost"}}'>
      <button class="typeBtn" bindtap="selCostType" style='background-color: {{costType=="food"? "#009CE9" : "white" }}'
        id="food">餐饮</button>
      <button class="typeBtn" bindtap="selCostType" style='background-color: {{costType=="dUse"? "#009CE9" : "white" }}'
        id="dUse">日用</button>
      <button class="typeBtn" bindtap="selCostType" style='background-color: {{costType=="trans"? "#009CE9" : "white" }}'
        id="trans">交通</button>
    </view>
    <view class="typeBox" wx:else>
      <button class="typeBtn" bindtap="selIncomeType" style='background-color: {{incomeType=="salary"? "#009CE9" : "white" }}'
        id="salary">工资</button>
      <button class="typeBtn" bindtap="selIncomeType" style='background-color: {{incomeType=="bonus"? "#009CE9" : "white" }}'
        id="bonus">奖金</button>

    </view>
    <form bindsubmit="formSubmit">
      <view>
        <view class="titleHeader">金额</view>
        <input name="money" class="inputBox" type="digit" placeholder="请输入金额" value="{{initInput}}"  />
      </view>
      <view>
        <view class="titleHeader">备注</view>
        <input name="remark" class="inputBox" placeholder="请输入备注" value="{{initInput}}" />

      </view>
      <view class="HContainer" style="padding-left: 20rpx;">
        <button class="formBtn" form-type="submit">确定</button>
        <button class="formBtn" style="margin: 10rpx 20rpx;" bindtap="exitPage">取消</button>
      </view>
    </form>
  </view>
</page-container>